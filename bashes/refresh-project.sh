clear && cd ~/tasks/reach-network/project && [ -d "vendor/" ] && [ -f "composer.lock" ] && rm -rf vendor/ composer.lock && composer i && cd ~/tasks/reach-network &&
cd ~/tasks/reach-network/packages/advertisering-system && [ -d "vendor/" ] && [ -f "composer.lock" ] && rm -rf vendor/ composer.lock && composer i && cd ~/tasks/reach-network
